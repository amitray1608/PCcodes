//Ryuzaki
#include<bits/stdc++.h>
using namespace std;

const int MAXN = 110;
int ar[MAXN];
int n;

void sine() {
  cin >> n;
  for(int i = 0; i < n; ++i) {
    cin >> ar[i];
  }

  
}

int main(){
  ios_base::sync_with_stdio(false);
  cin.tie(nullptr); cout.tie(nullptr);
  int t = 1;
  cin >> t;
  while(t--) sine();
  return 0;
}
