#include<bits/stdc++.h>
using namespace std;

struct Node{
	int v;
	Node *left, *right;
}

Node* insert(struct Node *root, int d){
	if(root == NULL){
		Node *node = new Node;
		Node->v = d;
		 root->v = d;
	}else if(root->v > d){
		return 
	}
}
int main(){
	
	return 0;
}
