#include<bits/stdc++.h>

using namespace std;

using ll = long long;

int main(){
  int n, m;
  cin >> n >> m;
  vector<int> a(m);
  for(int &i : a) cin >> i;
  sort(a.begin(), a.end());
  ll maxi = LONG_MAX;
  auto check = [&](ll x) {
    
  }
  return 0;
}
