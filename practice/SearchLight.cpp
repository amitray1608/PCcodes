//Ryuzaki
#include<bits/stdc++.h>
using namespace std;

vector<pair<int, int>> rob, light;

int recc(int i, 

void sine() {
  int n, m;
  cin >> n >> m;
  rob.resize(n);
  light.resize(m);
  for(auto &i : rob) cin >> i.first >> i.second;
  for(auto &i : light) cin >> i.first >> i.second;
  
}

int main(){
  ios_base::sync_with_stdio(false);
  cin.tie(nullptr); cout.tie(nullptr);
  int t = 1;
  //cin >> t;
  while(t--) sine();
  return 0;
}
